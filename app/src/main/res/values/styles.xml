<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/moneyBackground</item>
        <item name="colorPrimaryDark">@color/moneyBackgroundDark</item>
        <item name="colorAccent">@color/moneyBackground</item>
    </style>

</resources>
